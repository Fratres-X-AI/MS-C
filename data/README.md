# Data

v0.1 uses **procedural synthetic fixtures** in `models/scenes.py` (no large image
binaries). Seeds are locked via `sim/reproduce.py` (`GOLDEN_SEED=7`).

Future: partner site stills / IR panels land here under a data policy — never
commit controlled site vulnerability packages without counsel.
